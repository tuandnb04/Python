# 1. Khai báo biến (Dùng toán tử gán '=')
user_name = 'John Doe'
user_age = 25
print(user_name, user_age)

# 2. Quy tắc đặt tên biến (Rules):
# - Bắt đầu bằng chữ cái hoặc dấu gạch dưới (_), không bắt đầu bằng số.
# - Chỉ chứa chữ cái (a-z, A-Z), số (0-9) và dấu gạch dưới (_).
# - Không có giới hạn độ dài tên biến (không bị giới hạn 10 ký tự).
# - Phân biệt hoa/thường (Case-sensitive): age, Age, AGE là các biến khác nhau.
# - Không trùng với từ khóa của Python (reserved keywords như: if, class, def, v.v.).

# 3. Quy ước đặt tên (Conventions):
# - Dùng snake_case: các từ viết thường nối nhau bởi dấu gạch dưới (_)
# - Đặt tên có ý nghĩa rõ ràng, tránh đặt 1 chữ cái khó hiểu (như x = 56)
total_score = 100

# 4. Comments trong Python:
# Dùng dấu '#' cho comment đơn dòng hoặc nhiều dòng liên tiếp